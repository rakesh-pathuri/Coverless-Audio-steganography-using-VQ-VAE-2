Firebase Hosting uses this directory as the required public root.

The application itself is served by Cloud Run through the rewrite in firebase.json,
so this folder only exists to satisfy Firebase Hosting structure requirements.
